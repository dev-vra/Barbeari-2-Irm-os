[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma migrate deploy && (sh -c 'sleep 3 && npx ts-node --transpile-only prisma/seed.ts || echo Seed-failed' &) && node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
