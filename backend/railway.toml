[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma migrate deploy && sh -c 'npx ts-node --transpile-only prisma/seed.ts || echo \"Seed skipped\"' && node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
