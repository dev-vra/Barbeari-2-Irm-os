[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma migrate deploy && npx ts-node --transpile-only prisma/seed.ts && node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
